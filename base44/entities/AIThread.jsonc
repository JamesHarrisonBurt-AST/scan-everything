{
  "name": "AIThread",
  "type": "object",
  "properties": {
    "discovery_id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    }
  },
  "required": [
    "discovery_id"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}